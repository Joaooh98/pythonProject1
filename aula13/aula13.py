# comando len
# ultilizado para contar a quantidade de caracter retorna sempre um valor inteiro
usuario = input('digite seu usuario: ')
qtd_caracteres = len(usuario)
print(usuario, qtd_caracteres, type(qtd_caracteres))